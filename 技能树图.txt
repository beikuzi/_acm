acm：不得不说，用多版本来做acm简直就是神仙技能
	读入优化
	图论
		bfs
			SPFA
			dijk
			SPFA+dijk全源负环图最短路
			双向广搜
			最短路数量
			最短路还原
			差分约束
		Krus
		dfs
		tarjan
			从链到环上
		并查集cfind
		floyd
			最小环-无向-有向
		匈牙利算法
		网络流
			EK
			Dinic
			ISAP
			
		
	数据类型&数据结构
		st表
		树状数组
			最值
			单点前缀和
			单点值
			二维
		stl
			vector
			map<key, value>
		单调队列
		线段树
		bitset	
		
		
	算法思想
		搜索
			记忆化
		排序
			堆排
			并归
			快排
		dp
			背包
				多维
				分组
				依赖
				方案数
				具体方案
				单调队列优化'
			更新状态压缩成1维
				
		贪心
			线段合并	
			
		差分思想(区间批量增减的简易做法)
			普通差分
			二维差分
		倍增思想
		前缀和
		离散化+扫描线
		二分
		高精度
	数学
		1024特性
			给定n个数，都是2的a[i]次方（我们直接记录a[i]
			问是否可以合并成2个数，且这两个仍然是2的次方
				游戏可知：有且只有两个相同的合并，才可能构造下一个2次方的数，没有其他可能
				所以一直合并，最后能合成1-2个表示可以，>2不行
		凸包
			1.给出x个坐标，求出任选k个能得到的最大面积
				三角形的面积可以用任意两点叉积表示
					struct node{
						double x,y;
					}d[maxn];
					double cal(node a,node b){
						return a.x*b.y-a.y*b.x;
					}
				排列组合（此时用于计算概率，所以用小数点表示而并非需要准确值
					long double c[maxn][maxn];
					void deal(){
						for(int i=0;i<maxn;i++)c[0][i]=0,c[i][0]=1;
						for(int i=1;i<maxn;i++){
							for(int j=1;j<=i;j++){
								c[i][j]=c[i-1][j]+c[i-1][j-1];
							}
						}
					} 
			2.给出x个坐标，求把x个坐标都围起来的最短长度
		期望概率合并：
			用所有情况得出的期望相加，等价于先把所有情况累加出期望，再进行情况计算
		组合数奇偶问题：
			当 (n & m) == m 时，组合数C(m, n)为奇数
		对于任意的2/n, (n!=1), 
			都可以分解成1/n+1/(n+1)+1/(n*(n+1))
		对于任意的求乘积最大
			取对数ln后变成求对数和最大
		将一个整数分解成两段（包括1段）连续的数（并且不包括1
			连续加和，直到能造出第n个，第n+1个造不出来，且缺少k
			那么把第k个给去掉，保留n+1（由于1本身不存在，所以如果k==1，则去掉2，把n变成n+1
		逆元
		快速幂
		环问题
			对于环，取增加时(c+x)%n, 减少(c-x+n)%n；
			周期：从0开始记录，然后进行第i次时，用的是i%n
		最小公倍数和最大公因数
			gcd(x, y){
				return (!y)?x:gcd(y, x%y);
			}
			a*b/gcd(x, y)=最小公倍数
		辗转相除和更相减损
	图像处理
		镜像：设矩阵(m, n), 则m-x+1或n-y+1
		旋转：
			设矩形(m, n), 第x层	左上角(x, x)	右上角(,n -x+1)
							左下角（m-x+1, )	右下角()
			旋转后(n, m), 同样层 左上角(x, x)	右上角(,m -x+1)
							左下角（n-x+1, )	右下角()
			根据左旋或者右旋，对应位置，然后不同位置(x或y坐标)，用i=(x, n-x+1)代替原来原来的x
		
网页设计
	git
	hexo
	csdn
	css
	html
	js


数学
	积分
		曲线取值加和
		面积、体积积分
	向量法
		研究曲线上一点切向量和趋势
		最大梯度问题
		
线性代数
	矩阵